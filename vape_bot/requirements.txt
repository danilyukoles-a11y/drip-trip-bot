aiogram==3.27.0
supabase==2.28.3
httpx==0.28.1
python-dotenv==1.2.2
# requests is a transitive dep of supabase→storage3→pyiceberg,
# pinned here to ensure it's present when installing from scratch.
requests==2.33.1
